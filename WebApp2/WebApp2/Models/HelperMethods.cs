using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using WebApp2.Data;

namespace WebApp2.Models
{
    public static class HelperMethods
    {
        //public static async Task<FlightInfo> SearchFlight(FlightInfo flight) {
        //    FlightInfo retVal = _context.Flights.FirstOrDefault(x => (x.Triptype == flight.Triptype && x.Class == flight.Class && x.Persons == flight.Persons && x.City1 == flight.City1 && x.City2 == flight.City2));

        //    return retVal;
        //}
    }
}
